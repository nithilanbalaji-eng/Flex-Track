/**
 * Current privacy policy version. Must match the backend constant in
 * `backend/src/config/legal.ts` — bump both together when the policy changes
 * materially, and everyone will be asked to accept the new version.
 */
export const PRIVACY_POLICY_VERSION = "2026-08-10";
