/**
 * App Store product identifier. Must match the auto-renewable subscription
 * created in App Store Connect, and the id the backend checks receipts against.
 */
export const PREMIUM_PRODUCT_ID = "flextrack_premium_monthly";
