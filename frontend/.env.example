# Base URL of the Flex Track API. Leave as-is for local dev (Vite proxies /api to :4000).
VITE_API_URL=/api

# Google Sign-In client ID (same project as backend's GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=

# Sign in with Apple Services ID (same as backend's APPLE_CLIENT_ID)
VITE_APPLE_CLIENT_ID=
